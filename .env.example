PORT=5000
KEY=mykey

# Environment variables declared in this file are NOT automatically loaded by Prisma and current values are just as example.
# Please add `import "dotenv/config";` to your `prisma7.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgres://postgres:postgres@url:port/template?sslmode=disable"
SHADOW_DATABASE_URL="postgres://postgres:postgres@url:port/prisma_shadow?sslmode=disable"
